import { ContentTypeId, ContentCodec, EncodedContent } from '@xmtp/content-type-primitives';

declare const ContentTypeMarkdown: ContentTypeId;
declare enum Encoding {
    utf8 = "UTF-8",
    unknown = "unknown"
}
type TextParameters = {
    encoding: Encoding;
};
declare class MarkdownCodec implements ContentCodec<string, TextParameters> {
    get contentType(): ContentTypeId;
    encode(content: string): {
        type: ContentTypeId;
        parameters: {
            encoding: Encoding;
        };
        content: Uint8Array<ArrayBufferLike>;
    };
    decode(content: EncodedContent<TextParameters>): string;
    fallback(): undefined;
    shouldPush(): boolean;
}

export { ContentTypeMarkdown, Encoding, MarkdownCodec, type TextParameters };
